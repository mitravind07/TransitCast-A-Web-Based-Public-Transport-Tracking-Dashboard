// config.js — customize endpoints and keys
const CONFIG = {
  TRANSITLAND_BASE: "https://transit.land/api/v2/rest",
  // If you prefer TransportAPI or others, replace base URLs here.
  DEFAULT_RADIUS_METERS: 800,
  AUTO_REFRESH_SECONDS: 45,
  TRANSITLAND_ROUTING_URL: '',
  OTP_URL: '',
  VEHICLE_POSITIONS_URL: ''
};
